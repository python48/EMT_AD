<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="mx.iteso.sergio.emt_ad1.Analisis">


    <TextView
        android:layout_width="match_parent"
        android:layout_height="100dp"
        android:textSize="25dp"
        android:text="Cargando..."
        android:textColor="@color/letritas_grises"
        android:id="@+id/citaHeader"
        />


    <Button
        android:id="@+id/txtdate"
        android:onClick="showDatePickerDialog"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="20sp"
        android:text="Cargando..."
        android:textColor="@color/blanco"
        android:background="@color/colorPrimary"
        android:layout_gravity="left|center_vertical"
        android:layout_below="@+id/citaHeader"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true" />

    <!-- <Button
         android:id="@+id/botonsangres"
         android:text="picame.."
         android:onClick="sangreClick"
         android:layout_width="wrap_content"
         android:layout_height="wrap_content"
         android:textSize="20sp"

         android:layout_gravity="center_horizontal|top" />
 -->

    <!--
    android:hint="¡Agenda una visita!"
    -->

</RelativeLayout>
